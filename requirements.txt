openai>=1.40.0
sqlite-vec>=0.1.6
python-dotenv>=1.0.0
PyPDF2>=3.0.0
pycryptodome>=3.20.0          # AES-encrypted PDF decryption (PyPDF2 backend)
python-docx>=1.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Optional: cross-encoder reranker (highest-ROI retrieval upgrade). If absent,
# retrieval degrades gracefully to RRF order (see docrag/rerank.py). Pulls torch.
# Install with:  pip install FlagEmbedding
# FlagEmbedding>=1.2.0
